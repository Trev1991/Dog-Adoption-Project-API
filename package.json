{
  "name": "dog-adoption-api",
  "version": "1.0.0",
  "description": "Dog Adoption API with separation of concern, Petfinder v2 upstream, validation, and tests",
  "type": "module",
  "scripts": {
    "dev": "nodemon --config nodemon.json server.js",
    "start": "node server.js",
    "test": "NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules jest --runInBand"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0"
  }
}